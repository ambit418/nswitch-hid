[package]
name = "nswitch-hid"
version = "0.1.0"
edition = "2024"

[lints.clippy]
mod_module_files = "deny"

[features]
default = ["embassy"]
embassy = ["dep:embassy-usb"]

[dependencies]
bitflags = "2"
embassy-usb = { version = "0.4", features = ["usbd-hid"], optional = true }
